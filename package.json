{
  "name": "lectur-app-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run generate:firebase-config && astro dev",
    "build": "npm run generate:firebase-config && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate:firebase-config": "node scripts/generate-firebase-config.js",
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "npm run build && firebase deploy --only hosting",
    "serve": "firebase serve",
    "convert:cbr": "node scripts/convert-cbr-to-cbz.js",
    "generate:comics-json": "node scripts/generate-comics-json.js",
    "generate:stats": "node scripts/generate-content-stats.cjs"
  },
  "dependencies": {
    "astro": "^5.16.0",
    "dotenv": "^17.2.1",
    "firebase": "^12.1.0",
    "node-fetch": "^3.3.2"
  }
}
